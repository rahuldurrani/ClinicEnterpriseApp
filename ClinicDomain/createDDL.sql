CREATE TABLE Patient (ID BIGINT NOT NULL, BIRTHDATE DATE, NAME VARCHAR(255), PATIENTID BIGINT, PRIMARY KEY (ID))
CREATE TABLE Treatment (ID BIGINT NOT NULL, TTYPE VARCHAR(31), DIAGNOSIS VARCHAR(255), patient_id BIGINT, prov_id BIGINT, PRIMARY KEY (ID))
CREATE TABLE DRUGTREATMENT (ID BIGINT NOT NULL, DOSAGE FLOAT, DRUG VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE RADIOLOGY (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE SURGERY (ID BIGINT NOT NULL, SURGERYDATE DATE, PRIMARY KEY (ID))
CREATE TABLE Provider (ID BIGINT NOT NULL, PROVIDERID BIGINT, PROVIDERNAME VARCHAR(255), SPECIALIZATION VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE Radiology_RADIOLOGYDATES (Radiology_ID BIGINT, radiologyDates DATE)
ALTER TABLE Treatment ADD CONSTRAINT FK_Treatment_prov_id FOREIGN KEY (prov_id) REFERENCES Provider (ID)
ALTER TABLE Treatment ADD CONSTRAINT FK_Treatment_patient_id FOREIGN KEY (patient_id) REFERENCES Patient (ID)
ALTER TABLE DRUGTREATMENT ADD CONSTRAINT FK_DRUGTREATMENT_ID FOREIGN KEY (ID) REFERENCES Treatment (ID)
ALTER TABLE RADIOLOGY ADD CONSTRAINT FK_RADIOLOGY_ID FOREIGN KEY (ID) REFERENCES Treatment (ID)
ALTER TABLE SURGERY ADD CONSTRAINT FK_SURGERY_ID FOREIGN KEY (ID) REFERENCES Treatment (ID)
ALTER TABLE Radiology_RADIOLOGYDATES ADD CONSTRAINT FK_Radiology_RADIOLOGYDATES_Radiology_ID FOREIGN KEY (Radiology_ID) REFERENCES Treatment (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
